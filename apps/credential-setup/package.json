{
  "name": "@ai-dev-os/credential-setup",
  "version": "0.1.0",
  "private": true,
  "description": "Production-disabled hardened Windows Electron host with Stage 18E-I candidate-bound Anthropic validation.",
  "type": "module",
  "main": "dist/main/startup-bootstrap.cjs",
  "files": [
    "dist/main",
    "dist/preload",
    "dist/renderer",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../../packages/credential-ui run build && npm --prefix ../../packages/secrets-app-vault-electron run build && npm --prefix ../../packages/provider-anthropic run build && npm --prefix ../../packages/policy run build && npm --prefix ../../packages/secrets run build",
    "build": "node scripts/write-stage-18e-i-candidate-binding.mjs && tsc -p tsconfig.json && node scripts/copy-main-bootstrap.mjs && node scripts/copy-renderer.mjs",
    "pretest": "npm run prebuild",
    "test": "vitest run && node --test scripts/launch-production-host.regression.mjs",
    "pretest:coverage": "npm run prebuild",
    "test:coverage": "vitest run --coverage && node --test scripts/launch-production-host.regression.mjs",
    "pretypecheck": "npm run prebuild",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node scripts/launch-production-host.mjs",
    "ensure:electron": "node scripts/ensure-electron-runtime.mjs",
    "presmoke:real": "npm run build && npm run ensure:electron",
    "smoke:real": "node scripts/real-electron-smoke.mjs",
    "probe:startup-lifecycle": "node scripts/real-electron-startup-lifecycle-probe.mjs && node scripts/real-electron-visible-startup-probe.mjs",
    "preregenerate:evidence-screenshot": "npm run build && npm run ensure:electron",
    "regenerate:evidence-screenshot": "node scripts/real-electron-smoke.mjs --regenerate-evidence",
    "preverify:packed": "npm run build && npm run ensure:electron",
    "verify:packed": "node scripts/verify-packed-host.mjs"
  },
  "dependencies": {
    "@ai-dev-os/credential-ui": "^0.1.0",
    "@ai-dev-os/domain": "^0.1.0",
    "@ai-dev-os/policy": "^0.1.0",
    "@ai-dev-os/provider-anthropic": "^0.1.0",
    "@ai-dev-os/secrets": "^0.1.0",
    "@ai-dev-os/secrets-app-vault": "^0.1.0",
    "@ai-dev-os/secrets-app-vault-electron": "^0.1.0"
  },
  "devDependencies": {
    "electron": "43.4.1"
  },
  "peerDependencies": {
    "electron": "43.4.1"
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.0.0"
  }
}
