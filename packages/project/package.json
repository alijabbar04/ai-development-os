{
  "name": "@ai-dev-os/project",
  "version": "0.1.0",
  "private": true,
  "description": "Pure, deterministic, production-disabled canonical project contracts and projections for AI Development OS.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../domain run build && npm --prefix ../policy run build && npm --prefix ../scheduler run build && npm --prefix ../process-broker run build && npm --prefix ../secrets run build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "npm run prebuild",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "vitest run",
    "pretest:coverage": "npm run build",
    "test:coverage": "vitest run --coverage",
    "verify:packed-consumer": "node scripts/verify-packed-consumer.mjs"
  },
  "devDependencies": {
    "@ai-dev-os/domain": "^0.1.0",
    "@ai-dev-os/policy": "^0.1.0",
    "@ai-dev-os/process-broker": "^0.1.0",
    "@ai-dev-os/scheduler": "^0.1.0",
    "@ai-dev-os/secrets": "^0.1.0"
  }
}
