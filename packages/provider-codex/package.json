{
  "name": "@ai-dev-os/provider-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Codex App Server coding-agent adapter for AI Development OS with supervised JSONL transport, conservative sandbox and approval translation, workspace reconciliation, and account usage telemetry.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "prebuild": "npm --prefix ../workspace run build && npm --prefix ../scheduler run build",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm --prefix ../provider-testkit run build && npm --prefix ../workspace run build && npm --prefix ../scheduler run build",
    "test": "vitest run",
    "pretest:coverage": "npm --prefix ../provider-testkit run build && npm --prefix ../workspace run build && npm --prefix ../scheduler run build",
    "test:coverage": "vitest run --coverage",
    "pretypecheck": "npm --prefix ../provider-testkit run build && npm --prefix ../workspace run build && npm --prefix ../scheduler run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-dev-os/artifacts": "^0.1.0",
    "@ai-dev-os/domain": "^0.1.0",
    "@ai-dev-os/process-broker": "^0.1.0",
    "@ai-dev-os/providers": "^0.1.0",
    "@ai-dev-os/scheduler": "^0.1.0",
    "@ai-dev-os/workspace": "^0.1.0"
  },
  "devDependencies": {
    "@ai-dev-os/provider-testkit": "^0.1.0"
  }
}
