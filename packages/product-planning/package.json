{
  "name": "@ai-dev-os/product-planning",
  "version": "0.1.0",
  "private": true,
  "description": "Production-disabled bounded product-completeness planning over the existing task graph, persistence, provider, and scheduler contracts.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing/index.d.ts",
      "import": "./dist/testing/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../domain run build && npm --prefix ../persistence run build && npm --prefix ../providers run build && npm --prefix ../scheduler run build && npm --prefix ../task-graph run build",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm --prefix ../domain run build && npm --prefix ../persistence run build && npm --prefix ../persistence-memory run build && npm --prefix ../persistence-sqlite run build && npm --prefix ../providers run build && npm --prefix ../provider-anthropic run build && npm --prefix ../provider-testkit run build && npm --prefix ../scheduler run build && npm --prefix ../task-graph run build",
    "test": "vitest run",
    "pretest:coverage": "npm --prefix ../domain run build && npm --prefix ../persistence run build && npm --prefix ../persistence-memory run build && npm --prefix ../persistence-sqlite run build && npm --prefix ../providers run build && npm --prefix ../provider-anthropic run build && npm --prefix ../provider-testkit run build && npm --prefix ../scheduler run build && npm --prefix ../task-graph run build",
    "test:coverage": "vitest run --coverage",
    "pretypecheck": "npm --prefix ../domain run build && npm --prefix ../persistence run build && npm --prefix ../providers run build && npm --prefix ../scheduler run build && npm --prefix ../task-graph run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-dev-os/domain": "^0.1.0",
    "@ai-dev-os/persistence": "^0.1.0",
    "@ai-dev-os/providers": "^0.1.0",
    "@ai-dev-os/scheduler": "^0.1.0",
    "@ai-dev-os/task-graph": "^0.1.0"
  },
  "devDependencies": {
    "@ai-dev-os/persistence-memory": "^0.1.0",
    "@ai-dev-os/persistence-sqlite": "^0.1.0",
    "@ai-dev-os/provider-anthropic": "^0.1.0",
    "@ai-dev-os/provider-testkit": "^0.1.0"
  }
}
