{
  "name": "@ai-dev-os/context",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic context selection and packing over repository-index entries, memory records, and artifact references under explicit budgets.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing/contract-suite.d.ts",
      "import": "./dist/testing/contract-suite.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../repository-index run build && npm --prefix ../memory run build",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm --prefix ../repository-index run build && npm --prefix ../memory run build",
    "test": "vitest run",
    "pretest:coverage": "npm --prefix ../repository-index run build && npm --prefix ../memory run build",
    "test:coverage": "vitest run --coverage",
    "pretypecheck": "npm --prefix ../repository-index run build && npm --prefix ../memory run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-dev-os/artifacts": "^0.1.0",
    "@ai-dev-os/domain": "^0.1.0",
    "@ai-dev-os/memory": "^0.1.0",
    "@ai-dev-os/repository-index": "^0.1.0"
  },
  "peerDependencies": { "vitest": ">=4.0.0" },
  "peerDependenciesMeta": { "vitest": { "optional": true } }
}
