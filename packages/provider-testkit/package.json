{
  "name": "@ai-dev-os/provider-testkit",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic fake inference and coding-agent providers, manual scheduler, and standard contract-suite harnesses for AI Development OS.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "prebuild": "npm --prefix ../providers run build",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm --prefix ../providers run build",
    "test": "vitest run",
    "pretest:coverage": "npm --prefix ../providers run build",
    "test:coverage": "vitest run --coverage",
    "pretypecheck": "npm --prefix ../providers run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-dev-os/artifacts": "^0.1.0",
    "@ai-dev-os/domain": "^0.1.0",
    "@ai-dev-os/providers": "^0.1.0"
  }
}
